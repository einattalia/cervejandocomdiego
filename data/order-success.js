(()=>{'use strict';const params=new URLSearchParams(location.search);const order=params.get('pedido')||'';const status=params.get('status')||'success';document.getElementById('orderCode').textContent=order||'Confirmando…';const text=document.getElementById('statusText');if(status==='cancelled')text.textContent='O pagamento não foi concluído. Seu pedido continua como pendente e nenhuma baixa definitiva de estoque será feita.';else text.textContent='Recebemos seu pedido. A confirmação do pagamento é feita automaticamente pelo provedor, sem depender desta tela.';if(order){const a=document.getElementById('whatsappLink');a.href=`https://wa.me/5516997927171?text=${encodeURIComponent(`Olá, Diegão! Quero falar sobre o pedido ${order}.`)}`;}})();